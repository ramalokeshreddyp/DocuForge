services:
  - type: web
    name: docuforge-api
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: DATABASE_NAME
        value: wikidocs
      - key: MONGO_URI
        sync: false